---
import wordmark from "@/assets/img/logo-wordmark.png";

// Served straight from public/ rather than bundled — these are large video
// files, not worth running through the JS asset pipeline. Base-prefixed by
// hand so the path still resolves under the /TestWebsiteHE/ deploy subpath.
const base = import.meta.env.BASE_URL.replace(/\/+$/, "");
const clipA = `${base}/vid/clipA.mp4`;
const clipB = `${base}/vid/ClipB.mp4`;
---

<section class="hero">
  <video
    class="hero-video is-visible"
    data-hero-video="a"
    src={clipA}
    autoplay
    muted
    playsinline
    preload="auto"
  ></video>
  <video
    class="hero-video"
    data-hero-video="b"
    src={clipB}
    muted
    playsinline
    preload="auto"
  ></video>

  <div class="hero-scrim"></div>

  <div class="hero-content">
    <img
      src={wordmark.src}
      width={wordmark.width}
      height={wordmark.height}
      alt="Hotel Espir"
      class="hero-wordmark"
    />
    Fear is a place. Hotel Espir is a place. And you are not welcome here.
    <!-- TODO: swap "#" for the real Steam store page URL once available -->
    <a
      class="hero-cta"
      href="https://store.steampowered.com/app/4216950/Hotel_Espir/"
      target="_blank"
      rel="noopener noreferrer"
    >
      Wishlist us on Steam
    </a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    width: 100%;
    height: min(85vh, 56rem);
    min-height: 26rem;
    overflow: hidden;
    background: var(--color-background);
    /* Nav is transparent and sits above this in the stacking order (its
       z-index creates a stacking context), so pulling the hero up by
       exactly the nav's height lets the video show through behind it
       instead of starting only after the nav's own box. */
    margin-top: calc(-1 * var(--nav-height));
  }

  .hero-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0;
    transition: opacity 1s ease;
  }

  .hero-video.is-visible {
    opacity: 1;
  }

  .hero-scrim {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      rgba(33, 19, 17, 0.15) 0%,
      rgba(33, 19, 17, 0.35) 55%,
      rgba(33, 19, 17, 0.45) 75%,
      rgba(33, 19, 17, 1) 100%
    );
  }

  .hero-content {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: clamp(1.5rem, 4vw, 2.5rem);
    text-align: center;
    padding-top: 6rem;
    padding-right: 1.5rem;
    padding-left: 1.5rem;
  }

  .hero-wordmark {
    width: min(90%, 26rem);
    height: auto;
    filter: drop-shadow(0 6px 24px rgba(0, 0, 0, 0.65));
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.9rem 2rem;
    background: var(--color-accent);
    color: var(--color-accent-foreground);
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-family: "Donau Neue", system-ui, -apple-system, sans-serif;
    font-weight: 700;
    font-size: 0.9rem;
    border-radius: 999px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }

  .hero-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.45);
  }

  /* On-load entrance, skipped entirely for anyone who prefers reduced
     motion — they just get the final state with no animation. */
  @media (prefers-reduced-motion: no-preference) {
    .hero-wordmark {
      opacity: 0;
      animation: hero-fade-in 1.1s ease 0.2s both;
    }

    .hero-cta {
      opacity: 0;
      animation: hero-float-up 0.7s ease 0.9s both;
    }

    @keyframes hero-fade-in {
      to {
        opacity: 1;
      }
    }

    @keyframes hero-float-up {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  }
</style>

<script>
  function initHeroVideo() {
    const videoA = document.querySelector<HTMLVideoElement>(
      '[data-hero-video="a"]'
    );
    const videoB = document.querySelector<HTMLVideoElement>(
      '[data-hero-video="b"]'
    );
    if (!videoA || !videoB) return;

    let active = videoA;
    let next = videoB;

    function swap() {
      next.currentTime = 0;
      void next.play();
      next.classList.add("is-visible");
      active.classList.remove("is-visible");
      [active, next] = [next, active];
    }

    videoA.addEventListener("ended", swap);
    videoB.addEventListener("ended", swap);
    void videoA.play();
  }

  initHeroVideo();
  document.addEventListener("astro:after-swap", initHeroVideo);
</script>
